pipeline {
    agent any

    stages {
        stage('Getting Code') {
            steps {
                echo 'Got code form GIT'
            }
        }
        stage('Configure utilities') {
            steps {
                echo 'Installed'
            }
        }
        stage('Deployment') {
            steps {
                echo 'Copying the build to the server'
            }
        }
    }
}
